export const authStyle = {
  container: {

    flex: 1, // Make the container fill the entire screen
    padding: 20, // Add padding around all sides of the content
  },
  header: {
    fontSize: 24, // Set the font size for the header text
    marginBottom: 20, // Add margin below the header
  },
  textInput: {
    marginBottom: 10, // Add margin below each text input
    padding: 10, // Add padding inside the text inputs
    borderRadius: 5, // Add rounded corners to the text inputs
    borderWidth: 1, // Add a border to the text inputs
    borderColor: '#ccc', // Set the border color
  },
  button: {
    borderRadius: 10,
    backgroundColor: '#023E8A',
    height: 40,
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
  },
  secondaryButton: {
      borderWidth:1,
      borderRadius: 10,
      borderColor:"#023E8A",
      backgroundColor: '#ffffff',
      height: 40,
      display: 'flex',
      flexDirection: 'row',
      alignItems: 'center',
      justifyContent: 'center',
  
  },

  horizontalPadding:{
    paddingLeft:10,
    paddingRight:10
  }
};
